include:
  - project: "ironbank-tools/ironbank-modules"
    ref: main
    file: "/stages/release/gitlab-release.yaml"

version:
  when: always
  before_script:
    - echo "Using custom ci versioning script, skipping download"
