build:
  stage: build
  allow_failure: true
  # Need to convert from artifactory
  image: ${GITLAB_INTERNAL_REGISTRY}/dsop/ironbank-pipeline/all-in-one-fedora:1.0
  dependencies:
    - import-artifacts
  variables:
    ARTIFACT_DIR: ${ARTIFACT_FOLDER}/build
  before_script:
    - cp ${ARTIFACT_FOLDER}/import-artifacts/ .
  script:
    - buildah bud --format=docker ${CI_PROJECT_DIR}:$IMG_VERSION
  artifacts:
    when: always
    paths:
      - ${ARTIFACT_DIR}/
   

