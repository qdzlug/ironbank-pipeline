vat compare:
  dependencies:
    - load scripts
    - hardening_manifest
  tags:
    - ironbank
  variables:
    ARTIFACT_DIR: "${ARTIFACT_STORAGE}/lint"
  script:
    - set +e
    - 'python3 "${PIPELINE_REPO_DIR}/stages/lint/vat_findings.py"'
  allow_failure:
    exit_codes:
      - 3
      - 4
