twistlock-scan:
  image: registry1.dso.mil/ironbank-apps/rootless-twistcli:22.06.197
  stage: scanning

  # Load in the IMAGE_VERSION variable from load-scripts env
  dependencies:
    - load-scripts
    - build
    - scan-logic
  variables:
    # put all scan outputs in the scan-results folder
    VERSION_FILE: "${TWISTLOCK_SCANS}/twistlock-version.txt"
    CVE_FILE: "${TWISTLOCK_SCANS}/twistlock_cve.json"
    DETAIL_FILE: "${TWISTLOCK_SCANS}/twistcli-details.txt"

  before_script:
    - mkdir -p "${TWISTLOCK_SCANS}"
  script:
    - 'podman pull --authfile "${DOCKER_AUTH_CONFIG_FILE_PULL}" "${IMAGE_TO_SCAN}"'
    - 'twistcli --version >"${VERSION_FILE}"'
    - 'twistcli images scan --address "${TWISTLOCK_SERVER_ADDRESS}" --podman-path podman --custom-labels --output-file "${CVE_FILE}" --details "${IMAGE_TO_SCAN}" | tee "${DETAIL_FILE}"'
    - 'ls "${CVE_FILE}"'
    - 'chmod 0644 "${CVE_FILE}"'

  artifacts:
    when: always
    paths:
      - "${TWISTLOCK_SCANS}/"
    expire_in: 1 week
  retry:
    max: 2
    when: always
