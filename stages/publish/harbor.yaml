harbor:
  extends: .publish
  variables:
    IMAGE_FILE: "${CI_PROJECT_NAME}-${IMG_VERSION}"
    only:
     - master
  dependencies:
    - build
    - load scripts
    - hardening_manifest
  script:
    - '"${PIPELINE_REPO_DIR}/stages/publish/upload-to-harbor.sh"'
