harbor:
  extends: .publish
  variables:
    IMAGE_FILE: "${CI_PROJECT_NAME}-${IMG_VERSION}"
    # Removed for testing
    #only:
    #  - master
  dependencies:
    - build
    - load scripts
  script:
    - |
      if [[ "${SPECIAL_IMAGE_PATH}" ]]; then
        "${PIPELINE_REPO_DIR}/stages/publish/special-upload-to-harbor.sh"
      fi
    - '"${PIPELINE_REPO_DIR}/stages/publish/upload-to-harbor.sh" "${TEST_REGISTRY_URL}/${IM_NAME}" "${IMG_VERSION}"'
