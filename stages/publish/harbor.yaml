harbor:
  extends: .publish
  only:
    - master
  dependencies:
    - build
    - load scripts
    - hardening_manifest
  script:
    - ls "${PIPELINE_REPO_DIR}/stages/publish/"
    - '"${PIPELINE_REPO_DIR}/stages/publish/upload_to_harbor.py"'
